#include <stdio.h>

int main()
{
    int a = 100, b = 1;

    if (a > b)
    {
        print("%d, %d", a, b);

        if (a / b > 20)
        {
            if (a = a / 2)
            {
                /* code */
            }
        }
        while (b < (a / 2))
        {
            b++;
            printf("%d", b);
        }
    }
    else
    {
        print("%d, %d", a, b);
    }

    return 0;
}